//这是支持正则表达式的 String 对象的方法，所以访问的方法是str.search(string)
function myFunction(){
	var str="Mr. Blue has a blue house"
	var n=str.search("ha");
	document.getElementById("demo").innerHTML=n;
}
string.match(/regexp/igm)时未匹配到时并不会返回boolean值

//RegExp 对象方法  所以是regexp.exec(string)
var str="Hello world!";
//查找"Hello"
var patt=/hello/gi;
var result=patt.exec(str);
document.write("返回值: " +  result); 




/e/.exec("The best things in life are free!");




var patt = /e/;
patt.test("The best things in life are free!");

think  a item can grow up by the time

problem:

使用 JavaScript 来向 HTML 元素分配事件时

<body>

<p>点击按钮就可以执行 <em>displayDate()</em> 函数。</p>

<button id="myBtn">点击这里</button>
<p id="demo"></p>


<script>
document.getElementById("myBtn").onclick=function(){displayDate()}; //这个匿名函数的花括号里必须是要执行的函数名，不能在花括号里写创建的函数
function displayDate()
{
document.getElementById("demo").innerHTML=Date();
}
</script>
</body>
